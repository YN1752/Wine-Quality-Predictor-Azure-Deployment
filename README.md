# Wine Quality Predictor with Azure Deployment
- Created an ML pipeline, which runs all the codes, right from data ingestion to model evaluation.
- Implemented experiment tracking and model registration with MLFlow and DagsHub.
- Created Flask app with simple HTML that connects to MLFlow tracking uri, loads best model and displays prediction.
- Created a Docker image and ran the web interface in the Docker Container.
